{
  "layer": "default",
  "property": "CODE",
  "values": [
    {% for feature in m_urls.coll_items.features %}
      "{{ feature.properties.CODE }}"{% if not loop.last %},{% endif %}
    {% endfor %}
  ]
}
